{
  "description": "R3 imagen 1 con filas 5226 y tooltip",
  "selector": "body",
  "filter_grid_rows": [
    "5226_"
  ],
  "pre_capture_js": "const header = document.querySelector('gs-header'); if (header) header.style.display = 'none'; const panel = document.querySelector('otto-web-side-panel'); if (panel) panel.style.display = 'none'; window.dispatchEvent(new Event('resize')); const row = Array.from(document.querySelectorAll('otto-web-grid-row')).find(r => r.textContent.includes('5226_5')); if (row) { const blocks = row.querySelectorAll('gs-timeline-block'); const targetBlock = blocks[8]; if (targetBlock) { const targetBox = targetBlock.querySelector('.block__content__box'); if (targetBox) { targetBox.style.setProperty('background-color', '#0078d7', 'important'); targetBox.style.setProperty('border-color', '#0096ff', 'important'); } targetBlock.style.setProperty('--gs-timeline-block-background', '#0078d7', 'important'); targetBlock.style.setProperty('--gs-timeline-block-border', '#0096ff', 'important'); const tooltipContainer = document.querySelector('.cdk-overlay-connected-position-bounding-box'); if (tooltipContainer) { const blockRect = targetBlock.getBoundingClientRect(); const tooltipPane = tooltipContainer.querySelector('.cdk-overlay-pane'); const tooltipRect = tooltipPane.getBoundingClientRect(); const blockCenter = blockRect.left + (blockRect.width / 2); const tooltipLeft = blockCenter - (tooltipRect.width / 2); tooltipContainer.style.setProperty('left', tooltipLeft + 'px', 'important'); tooltipContainer.style.setProperty('top', 'auto', 'important'); const tooltipBottom = window.innerHeight - blockRect.top + 8; tooltipContainer.style.setProperty('bottom', tooltipBottom + 'px', 'important'); tooltipContainer.style.setProperty('align-items', 'flex-start', 'important'); tooltipContainer.style.setProperty('justify-content', 'flex-start', 'important'); tooltipContainer.style.setProperty('height', 'auto', 'important'); tooltipPane.style.setProperty('position', 'static', 'important'); } } } document.querySelectorAll(\"[transloco='vehicleRoster.timeline.event.type']\").forEach(function(span){var parent=span.parentElement;parent.childNodes.forEach(function(n){if(n.nodeType===3&&n.textContent.includes('Descanso')){n.textContent=n.textContent.replace('Descanso','Refeição');}});});",
  "bbox_mode": "smart",
  "padding": [
    0,
    0,
    10,
    0
  ],
  "viewport_width": 1600,
  "viewport_height": 950,
  "device_scale_factor": 1
}