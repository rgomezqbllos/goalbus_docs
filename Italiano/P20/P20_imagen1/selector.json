{
  "selector": "div.configuration__card:has(button[data-qa-id='driverManagement'])",
  "bbox_mode": "element",
  "description": "Tarjeta Gestión de Conductores (P20_imagen1)",
  "padding": 10,
  "pre_capture_js": "(() => { const btn = document.querySelector(\"button[data-qa-id='driverManagement']\"); if (!btn) return; const card = btn.closest('div.configuration__card'); const el = card || btn; el.scrollIntoView({block:'center', inline:'center'}); })();"
}
