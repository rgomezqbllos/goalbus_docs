{
  "description": "Tarjeta Reglas de Asignación, estable por data-qa-id del botón",
  "selector": "#codex-r4-img1-anchor",
  "bbox_mode": "element",
  "padding": 0,
  "viewport_width": 1920,
  "viewport_height": 1200,
  "device_scale_factor": 1,
  "pre_capture_js": "(() => { const card = document.querySelector(\"div.configuration__card:has(button[data-qa-id='rulesConfiguration'])\"); if (!card) return; card.scrollIntoView({block: 'center', inline: 'center'}); const r = card.getBoundingClientRect(); let anchor = document.querySelector('#codex-r4-img1-anchor'); if (!anchor) { anchor = document.createElement('div'); anchor.id = 'codex-r4-img1-anchor'; document.body.appendChild(anchor); } Object.assign(anchor.style, { position: 'fixed', left: `${Math.round(Math.max(0, r.left - 28))}px`, top: `${Math.round(Math.max(0, r.top - 14))}px`, width: '417px', height: '168px', opacity: '0', pointerEvents: 'none', zIndex: '2147483647' }); })();"
}
